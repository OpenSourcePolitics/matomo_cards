---
resource: referrers
query:
  model:  true
  sql: select 
            label, 
            nb_uniq_visitors,
            nb_visits,
            nb_actions,
            sum_visit_length,
            bounce_count,
            nb_visits_converted,
            sub_type, 
            date(date)
       from referrers
       where sub_type is null
  info:
    filterables:
    viz_settings:
      
