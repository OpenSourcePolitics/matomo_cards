---
resource: pages
query:
  model:  true
  sql:  |
    select 
        nb_visits::int, 
        nb_uniq_visitors::int,
        nb_hits,
        sum_time_spent,
        exit_nb_uniq_visitors::int,
        exit_nb_visits::int,
        avg_time_on_page, 
        bounce_rate, 
        exit_rate,
        url,
        date,
        entry_nb_uniq_visitors::int,
        entry_nb_visits::int,
        entry_bounce_count::int,
        label
    from matomo.pages
  info:
